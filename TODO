[x] Define classes for Petri nets and ODE systems similar to Bob's, but let's use Num in place of Ring to start.
[x] Write an instance of the PetriNet class given an instance of ODESystem that constructs an ODE for the given net description.
[ ] Write a concrete instantiation of ODESystem using polynomials so we can see the systems we're defining.
[ ] Encode a bunch of examples of Petri nets, especially ones for which we know what the mass action semantics are supposed to be.
[ ] Make tests that check the resulting ODEs.
[ ] (Dubiously useful) Make an Algol type class encoding simple imperative programs.
[ ] Write an instance of ODESystem that does Euler integration -- if we do the Algol type class, we can write it in terms of that. Otherwise,
    basically anything seems fine. Maybe could use IO or ST, but it's basically a pure computation.
[ ] Maybe put a little frontend on this so the user can define rules and transitions and graph the results of integration.

-- Post Oct 10 delivery as I see it, but we might get started earlier
[ ] Some other types of dynamical systems
[ ] Lenses between systems
[ ] Charts between systems
[ ] Maybe once we have those, there will be specific cases where we'll be able to check that some square of lenses and charts commutes,
      or produce a list of proof obligations.

